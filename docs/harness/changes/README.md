# changes/ — 变更追踪

每次对 `docs/harness/` 规范体系的修改都建立一条记录，编号递增：

```
NNNN-YYYYMMDD-<主题>.md
```

记录内容：
- 日期与触发来源（任务/需求编号）
- 补丁清单：改了哪些文件、什么内容、为什么
- 用户裁决：每条补丁批准/拒绝及理由
- 红线增删记录（红线变动必须在这里留痕）

第一条记录由 bootstrap 启用时创建，格式为 `0001-YYYYMMDD-初始化-harness.md`（日期为启用当天，与后续记录格式一致，便于排序与解析）。
