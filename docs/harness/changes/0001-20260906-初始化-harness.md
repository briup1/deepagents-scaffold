# 0001-20260906-初始化 harness

- 日期：2026-09-06
- 触发来源：harness-bootstrap-legacy 全量逆向生成（用户批准删除旧 docs/harness/ 空目录后重建）

## 补丁清单

| 文件 | 内容 | 理由 |
|------|------|------|
| `docs/harness/` 全部 16 文件 | 新建规范体系（agents/rules×3/skills×7/wiki×4/changes/README） | 从代码库逆向生成四层事实并提炼 |
| 项目根 `CLAUDE.md` | 置顶插入宪法区（0.1 技术栈 / 0.2 红线 11 条 / 0.3 文件索引）；修正环境变量第 5 条 `SCAFFOLD_API_KEY` 表述为 `auth.users` 机制 | 融合不覆盖；冲突条款（鉴权表述）经用户在结构确认阶段裁决以 `config.yaml` auth 段为准 |
| `docs/harness/CLAUDE.md` | 暂存稿删除 | 宪法只留项目根一份，避免双份漂移 |

## 用户裁决

- 结构层确认：通过（含技术栈表、红线三分类、技术债摘要、自检结果）。
- 内容层走查：通过，直接启用。
- 决策项：覆盖率核心模块 ≥60%（❓ 新提议）；不引入业务错误码体系；API 无版本前缀；核心链路 = `/agent` SSE / 抽取流水线 / 历史持久化；单函数 ≤80 行（❓）；回滚 = git revert + 重启。

## 红线增删记录

- 新增红线 11 条：✅×7（1、2、3、5、7、8、11）、⚠️×2（4、9 债务标注）、❓×2（9 脱敏机制、10 函数行数）。
- 红线 4、6、9 附技术债定位（sandbox `except: pass`×2、createCatalog `as any`×1、日志无脱敏），未修复，仅登记。
